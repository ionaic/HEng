import System.Environment

fib :: Int -> Int
fib 1 = 1
fib num = (fib $ num - 1) + (fib $ num - 2)

main = do 
        putStrLn "Hello World!"
        f <- getArgs
        putStrLn fib $ Read f :: Int

